//It is basically like Bottom to Top approach

// Hoisting is (to many developers) an unknown or overlooked behavior of JavaScript.

// If a developer doesn't understand hoisting, programs may contain bugs (errors).

// To avoid bugs, always declare all variables at the beginning of every scope.

// Since this is how JavaScript interprets the code, it is always a good rule.


TestName = "Selvan"
console.log(TestName) 

//************************************VAR************************************//

//var x
console.log(x) //It will print Undefined
var x="Thamarai"
console.log(x)

//************************************ LET/CONST ************************************//

//let y
console.log(y) //It will throw cannot access error
let y="Thamarai"
console.log(y)

//************************************ Real Time Example************************************//

//Pizza Ordering in Dominos as each custotmer will have each preferences but all the base will be ready. 